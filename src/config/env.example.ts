/**
 * Template for the git-ignored `env.generated.ts` (written by `scripts/gen-env.js` from
 * XTREAM_DNS1 / XTREAM_USERNAME / XTREAM_PASSWORD / M3U). Never put real credentials in this file.
 */
export const ENV = { baseUrl: '', username: '', password: '', m3u: '', label: 'My line' };
